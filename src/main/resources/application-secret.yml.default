spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: 'CLIENT ID'
            map-appKey: 'APP KEY'
  jpa:
    properties:
      hibernate:
        dialect: 'MYSQL'
  data:
    mongodb:
      uri: 'MONGODB'

custom:
  user:
    name: 'KAKAO USER NAME'
