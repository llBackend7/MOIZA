spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: 'CLIENT ID'
  jpa:
    properties:
      hibernate:
        dialect: 'MYSQL'
  data:
    mongodb:
      uri: 'MONGODB'

custom:
  user:
    name: 'KAKAO USER NAME'
            map-appKey: 'APP KEY'
  data:
    mongodb:
      uri:
