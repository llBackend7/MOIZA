spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: 'CLIENT ID'
            map-appKey: 'APP KEY'
  jpa:
    properties:
      hibernate:
        dialect:
    data:
      mongodb:
        uri: