spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  profiles:
    active: dev
    include: secret
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://192.168.56.101:3308/gram__dev?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Seoul
    username: testuser
    password: 5678
    #driver-class-name: org.mariadb.jdbc.Driver
    #url: jdbc:mariadb://127.0.0.1:3306/moiza__dev?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Seoul
    #username: lldj        # ?? DB ??
    #password: ${MYSQL_PW} # ?? DB ??
  security:
    oauth2:
      client:
        registration:
          kakao:
            scope: profile_nickname, account_email, profile_image
            client-name: Kakao
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            client-authentication-method: POST
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
  jpa:
    properties:
      hibernate:
        default_batch_fetch_size: 50
  sendgrid:
    api-key: ${SENDGRID_MOIZA}
custom:
  jwt:
    secretKey: thissecretkeyisfordevandtestdontusethissecretkeyinrealproduction
  site:
    baseUrl: